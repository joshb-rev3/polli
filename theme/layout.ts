/** Shared layout tokens for desktop-aware app screens. */

/** Breakpoint where app screens switch to centered content column. */
export const DESKTOP_BP = 900;

/** Max width for app content (feed, forms, chrome inners). Splash uses its own wider shell. */
export const CONTENT_MAX = 560;

/** Default horizontal padding inside Content. */
export const CONTENT_PAD = 20;

/** Marketing splash max shell width (homepage only). */
export const SPLASH_MAX = 1120;
